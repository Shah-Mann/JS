let mybio = {
  Name: "Mann",
  Age: 21,
  Location: "Ahmedabad",
};

console.log(`${mybio.Name} is ${mybio.Age} and lives in ${mybio.Location}`);

mybio.Age = 22;

console.log(`${mybio.Name} is ${mybio.Age} and lives in ${mybio.Location}`);
